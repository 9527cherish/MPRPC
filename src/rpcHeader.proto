syntax = "proto3";

// 定义命名空间
package mprpc;

// 使用protoc生成proto文件 
option cc_generic_services = true;

message RpcHeader
{
    bytes serviceName = 1;
    bytes methodName = 2;
    uint32 argsSize = 3;
}
